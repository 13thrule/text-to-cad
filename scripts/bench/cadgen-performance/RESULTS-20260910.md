# Cadgen performance study — 2026-09-10 UTC

## Current findings

The user stopped further tendon-hand stress tests on September 11. Ongoing
experiments and integrated acceptance use the nine-part planetary assembly,
modest repeated-parts fixtures and small curved components. Existing hand
results below remain diagnostic history; no further hand run is scheduled.

The current `8971f760d` viewer checkpoint combines selection retention fixes
with exact detail-publication ownership and recovery. All 13 moderate adaptive
browser assertions pass, with no page/HTTP errors, every detail target settled,
210.56 MiB peak renderer RSS and orbit frame interval p95 of 8.7 ms. The complete
nine-part screenshot was visually reviewed. The installed-wheel and source-free export checks pass on the same commit.
The [medium ownership checks](VIEWER-OWNERSHIP-INTEGRATED-20260910.md) pass
90 assertions; the alert-only follow-up `c07488e1d` passes 15 more. Bounded grouping
is now committed in `9be4f5424`, with 506 viewer tests and 32 combined browser
assertions passing. The [moderate batch comparison](VIEWER-LOD-BATCH-MODERATE-20260910.md)
reduces six scene publications to two. Compared with the same candidate at size
one, size four completes refinement about 42 ms sooner, with the first refined
draw 7–11 ms later. These three-sample medians concern cached browser refinement,
not generation or saved STEP latency. The combined screenshot was visually
reviewed. The [isolated installed-wheel asset check](results/installed-wheel-viewer-9be4f5424-20260911.json.gz)
also passes for this client. Backend changes require their own final validation.

The [matched before/after study](WARM-COMPARISON-20260910.md) compares reviewed
`7aa3e85be` with the latest validated backend, separating previously emitted
values from new edits. Repeated imported-STEP builds improve from roughly
0.86 seconds to 0.38–0.42 seconds. New imported edits improve to 0.65–0.74 seconds
and publish source previews in 153–195 ms. New procedural previews remain
near 500 ms, and new split-model saves regress by roughly 10–15%; those limits
remain open. Repeated public STEP reads improve from 258 to 32 ms, while the
first empty-store read regresses to 4.20 seconds including compile startup.

The [mesher v2 experiment](MESHER-V2-20260910.md) retains the JS mesher after
measuring the complete native extraction path and auditing two bounded corpora.
The [FreeCAD comparison](WARM-COMPARISON-20260910.md#freecad-on-the-same-step) measures
headless retained-document editing separately from whole-file persistence;
it establishes an architectural gap, not matched GUI/render performance.

Current browser work and acceptance are recorded in the
[memory diagnosis](VIEWER-MEMORY-DIAGNOSTIC-20260910.md),
[matched nine-part viewer study](VIEWER-NINE-BEFORE-AFTER-20260910.md), and
[validation record](VALIDATION-20260910.md). The [installed-wheel and source-free
export checks](results/installed-wheel-8971f760d-20260911.json.gz) pass at `8971f760d`:
182 Python and 29 runtime files match source, wheel and isolated installation.
Later runtime changes require their relevant checks again.
The isolated selection fix now passes the full-hand 2 GiB/detail gate; the
combined runtime is being validated on moderate assemblies. Its complete canonical L1 census
requires 426 MiB of display buffers plus 20 MiB of edge textures, before GPU
mirrors, application objects and transient allocations. That footprint does
not establish that the browser target is infeasible.
The [current adaptive checkpoint](results/hand-default-adaptive-settled-tree-20260910.json.gz)
keeps all 866 components and 3,259 occurrences displayed, with first geometry at
791 ms, complete initial publication at 13.962 seconds, peak renderer RSS
1,613.5 MiB and orbit p95 25.0 ms. Close-up refinement misses the 30-second
settling deadline: 33 uncached fine-mesh requests consume 22.79 seconds of
worker time, and 63 targets remain unmet. Return to the original view,
selection/clear and cleanup pass. The medium assembly passes all 13 adaptive
checks (233 ms first geometry, 657 ms complete, 204.6 MiB peak).

The preceding forced-L1 stress reached 654 L1 / 212 L0 before exceeding 2 GiB.
A [matched medium-assembly retention study](VIEWER-SELECTION-RETENTION-20260911.md)
now confirms a repeated-detail UI retention chain. Preserving unchanged
selection arrays reduces strongly reachable workspace contexts from 503 to 9
and assembly maps from 250 to 5 after the same 246 detail adoptions. Retained
backing storage after collection falls from 19.953 to 3.393 MB. A separate
[full-hand test](results/hand-canonical-l1-selection-identity-summary-20260910.json)
passes all nine assertions: 866 L1 components and 3,259 occurrences in 97.581
seconds, with a 1,482 MiB peak renderer and no pre-grade collection. Scene
ownership settles with no pending reservations or live workers. The isolated
candidate includes only the selection fix over `2709968dc`; scene-recovery
changes are excluded. The medium diagnostic peaks are not ordinary-viewing RSS
measurements. All L0/L1 entries were cached; fine-level caches were
partially warm. These successive browser checkpoints are not matched speedup
measurements, and possible external host work or use of the separate play
viewer remains a limitation.

A [C++ GLB packing experiment](RWGLTF-OPPORTUNITY-20260910.md) revisits native
meshing without Python per-vertex extraction. Its private adapter passes exact
edge/selector correspondence and repeated-byte checks. A uniform half-chord
margin meets finite sampled quality targets on the
[moderate corpus](RWGLTF-MODERATE-QUALITY-20260910.md), but its curved-part costs
do not justify replacing the current mesher. Native integration is deferred;
no production backend or tessellation identity changed in this experiment.
Earlier sections below preserve their original measured runtimes and cache
states; their timings are historical checkpoints, not current validation.

## Initial study checkpoint

Keep the current canonical JS mesher. Native OCCT meshing is faster in its
kernel stage on this fixture, but Python extraction consumes that saving at
the default LOD. The study does not demonstrate equal visual quality or an
end-to-end viewer improvement. The warm-build changes publish a preview about
0.41 seconds before the saved result, but the 250 ms preview target is not met.

## Workload and environment

- Apple M1 Max, 10 logical CPUs, 64 GiB RAM, macOS 26.5.1 arm64.
- Python 3.13.13; build123d 0.11.1; cadquery-ocp 7.9.3.1.1;
  numpy 2.5.2; Node v26.7.0; cadgen distribution version 0.5.1.
- Git HEAD `7aa3e85be76f305437abd3d7aba26e38b28e43cb` with working changes.
  Runtime source digests distinguish the actual measured source below.
- Existing nine-part planetary fixture copied under `models/tmp/perf-audit`.
  Its only output is `planetary.step`; mesh-export decorators and the kinematics
  sidecar are omitted. The source SHA-256 is
  `34b6ff6b337d8ee986e500b43a00102da421c868b61b39b678b4dc498554b64f`.
- Baseline output: 2,268,663 STEP bytes; 9 occurrences and 9 unique components;
  582 faces and 1,692 edges. Meshing inputs total 1,866,910 SURF bytes and
  393,300 BREP bytes. Surfaces are planes and cylinders with trimmed loops,
  holes, and seams. The fixture does not cover B-splines or singular surfaces.
- Fixed input tree
  `d34345bdc0a7a6a030f34af8872d6ad9a6ab43f24ca5bcfa83e00edb2e298e01`;
  STEP SHA-256
  `406dd2e19b6a4ea3a623abd19b03708b7d9e97b4402ba4161c737eb31f10fdca`.
  Later model/store updates do not change the copied native-study input view.

The warm and native trials ran in coordinated idle windows, without profiling,
competing CAD/browser activity, or runtime source changes during either study.
The final native trial supersedes the preliminary local attempts made while
checking the experimental codec's side-ordinal convention.

## Warm model execution

[Raw warm results](results/warm-20260910.json) contain every sample, runner
event, stage, cache delta, and content identity. Their sibling log directory
contains the normal verbose output. The runtime source digest was
`254e0473394ad4b0e518cc14d1c22fc86fbabd2613cb0ebad90606a88755a9d2`.
`sourceRestored` and `runtimeUnchangedDuringStudy` are both true.

Times are milliseconds in one interpreter with the kernel already imported.
Each source variant was primed before three measured repetitions. Source
writes, startup, daemon IPC, and all browser work are excluded.

| Workload | Minimum | Median | Maximum | Changed component IDs |
| --- | ---: | ---: | ---: | ---: |
| Unchanged source | 22.14 | 23.74 | 23.76 | 0 |
| Carrier diameter 105 → 106 mm | 958.52 | 972.28 | 1,114.59 | 1 replaced |
| Carrier placement −0.5 mm in Z | 956.86 | 969.79 | 977.75 | 0 |

The geometry and placement runs each reported 31 operation-cache hits and zero
misses. Placement reuse preserves every component ID, but the model body and
STEP persistence pipeline still execute. Peak process RSS reached about
580 MiB; this is a high-water mark including imported runtime/kernel state.

| Backend event | Geometry edit median (range) | Placement edit median (range) |
| --- | ---: | ---: |
| First preview publication | 561.43 (536.63–678.19) | 552.83 (543.44–561.64) |
| Saved publication | 967.09 (953.06–1,109.25) | 964.07 (950.17–971.90) |
| Preview lead over save | 416.44 (405.66–431.05) | 410.26 (406.74–411.24) |

These are backend publication events, not first visible browser geometry.
Because preview publication itself takes more than 500 ms, this result already
misses the under-250-ms visible-preview goal before browser costs are added.

The normal stage logs attribute roughly 426–455 ms to model-body execution,
9–10 ms to tree preparation, 9–10 ms to preview publication, 124–137 ms to
STEP assembly/write, and 255–273 ms to STEP read-back. Component-packaging
timings include nested work and must not be added again to the body/STEP totals.
The preview callback removes the final STEP work from the earliest publication
path, but model execution remains the main obstacle to the preview target.

The [earlier baseline data](results/baseline-warm-20260910.json), retained
unchanged from the local `baseline/warm_results.json` probe, recorded unchanged
calls at 20.44 and 23.77 ms, a cached carrier edit at 947.15 ms, and its revert at
927.53 ms. That probe used the same source substitution and in-process timing
boundary, but recorded fewer samples and no preview events/source digest.
These results do not establish a warm-build throughput improvement. The useful
measured difference is earlier preview publication while persistence finishes.

### Repeated public STEP imports

The input is the same 2.27 MB STEP, copied into an initially empty private store.
The cold public call took **7,562.80 ms**, including its compile subprocess
startup. Warm calls took **31.74, 34.19, and 42.41 ms**, with compile-job
submission explicitly forbidden. All returned volume
`100976.60270468247 mm³`. This demonstrates the public hit path avoiding a
compile on these calls; it is not a fresh-caller startup benchmark.

## Final runtime confirmation

After integration and browser checks finished, a second isolated warm study ran
against committed runtime `43b233233bc3ec19b1a90eb53acf629192aae294`. The task's
viewer and preview daemon were stopped first; no other task tests or CAD
measurements ran concurrently. [Final raw results](results/warm-final-20260910.json)
use the same fixture, priming, three repetitions, kernel-warm timing boundary
and source substitutions. Source restoration and the unchanged-runtime check
both pass. Runtime fingerprint:
`12dcce949534a0a3e65649e72cfa89b3957e604d7e900a49b47e4772623aeb61`.

| Final workload | Minimum | Median | Maximum |
| --- | ---: | ---: | ---: |
| Unchanged build | 18.00 ms | 18.34 ms | 19.57 ms |
| Cached geometry edit | 925.91 ms | 926.20 ms | 935.97 ms |
| Cached placement edit | 924.64 ms | 927.67 ms | 929.59 ms |
| Geometry preview publication | 520.67 ms | 523.37 ms | 529.61 ms |
| Placement preview publication | 524.52 ms | 527.56 ms | 528.93 ms |
| Geometry saved publication | 920.43 ms | 921.13 ms | 930.30 ms |
| Placement saved publication | 919.46 ms | 922.19 ms | 924.27 ms |

Repeated imports take 28.49–30.23 ms (median 30.06 ms), with compile submission
forbidden and the same returned volume. Cold import including its subprocess
startup is 6,690.81 ms. These final values confirm the earlier conclusion:
preview is available roughly 0.40 seconds before saving finishes, but the
250 ms target remains unmet. The limited earlier baseline still does not support
a statistically meaningful claim of faster complete warm builds. The native
experiment below is unchanged and remains a separate study.

The later browser-only cancellation patch is measured separately on
`947f1dade`. One cold/cached pair opens the nine-part assembly with a
first-geometry frame proxy of 367/263 ms and peak largest-renderer RSS of
279/168 MiB. A 24-occurrence fixture retains its GPU allocation plateau through
six switches and records p95 browser frame interval 9.9 ms during a five-second
orbit. These are bounded browser checks, not large-hand or before/after speedup
claims. [Validation details](VALIDATION-20260910.md#final-browser-confirmation)
record the cache state, source/record independence, settings and metric limits.

## Resumed implementation measurements

The earlier integration record did not complete the plan. The resumed work
adds exact surface reuse across revisions, an explicit initial coarse tier,
progressive-load failure fencing and pressure recovery, stronger native
mutation checks, and retention of the authored viewport after an explicit save.

[Current warm results](results/warm-resumed-20260910.json) use the same nine-part
fixture and three repetitions, with runtime source fingerprint
`7d10bfde6bc2692bc74adef5be2ef012098e9005859ef6c37889b5293fea07a7`.
Source restoration and the unchanged-runtime check both pass.

| Current workload | Median | Range |
| --- | ---: | ---: |
| Unchanged build | 18.09 ms | 17.32–18.61 ms |
| Geometry edit | 843.19 ms | 833.50–929.03 ms |
| Placement edit | 844.34 ms | 843.65–856.06 ms |
| Geometry preview publication | 464.77 ms | See raw events |
| Placement preview publication | 458.97 ms | See raw events |
| Repeated public import, compile forbidden | 27.92 ms | 26.56–30.07 ms |

Cold import including process startup took 6.16 seconds. The 250 ms preview
target remains unmet. These are separate runs from the earlier committed
confirmation, not an interleaved end-to-end speedup experiment.

The [signature-only interleaved A/B](results/signature-ab-20260910.json) isolates
the exact-signature optimization: model-body median 434.5 → 401 ms, with
identical saved and preview trees. Diagnostic signature time fell from roughly
212–220 to 158–161 ms across 9,282 calls. Preview medians stayed near 538 ms;
the noisy total changed from 946 to 975 ms. That A/B supports a cheaper
signature calculation, not a faster total save or a met preview target.

The diagnostic hand attempts did **not** demonstrate complete loading:

| Attempt | First geometry frame | Full load | Largest renderer peak |
| --- | ---: | --- | ---: |
| [Initial coarse tier](results/hand-coarse-20260910.json) | 3,164 ms | Partial; original false completion corrected | 1,354 MiB |
| [Pressure recovery, before failure-fence fix](results/hand-recovery-20260910.json) | 2,879 ms | Timed out at 100 seconds | 1,881 MiB |
| [Failure fence](results/hand-integrated-20260910.json) | 2,669 ms | Partial; admission failure | 1,207 MiB |
| [Release-epoch recovery](results/hand-release-epoch-20260910.json) | 2,727 ms | 504 components published at timeout | 1,583 MiB |
| [Per-worker accounting](results/hand-worker-slots-20260910.json) | 2,770 ms | 504 components published at timeout | 1,511 MiB |

The fixture has 866 unique components, 3,259 occurrences and 279,285,151 STEP
bytes. Both attempts began with an empty mesh index in the task-owned store.
The [short functional trace](results/hand-reset-diagnostic-20260910.json)
identified a recoverable memory denial followed by late sibling publication:
after one load failed, other lanes published inconsistent partial counts and
erased the terminal error, causing retries. There was one immutable assembly
request and no abort call; the document revision did not change. The fix drains
and fences all lanes, and allows pressure recovery again after actual admitted
progress. It does not increase the budget. These failed runs are retained as
diagnostic evidence, not large-hand acceptance results.

The last two runs continue forward without admission errors, but do not finish
within 100 seconds. Per-worker accounting charges the actual slot's high-water
estimate, not the largest job multiplied by completed calls. It removes a
source of unnecessary pressure without raising the budget.

A [kernel-free route probe](results/component-route-20260910.json) found that
each component HEAD/GET rebuilt the full 3,259-occurrence descriptor. Five
lookups took 37.0–51.7 ms each. Resolving only the immutable component/link maps
takes 11.6–12.0 ms; assembly.json still uses the full composition. These are
separate interpreter invocations on the same host and warm filesystem, not a
browser timing comparison. The new lookup still validates the root, linked
trees and requested object, rejects conflicting mappings, and observes store
deletion without a persistent or process descriptor cache.

### Complete large-hand check

The final [cold run](results/hand-route-final-20260910.json) and
[cached companion](results/hand-route-cached-20260910.json) both render all
866 components and 3,259 occurrences. The completion gate checks the actual
scene counts and a scene synchronization after the final publication. Both use
the same 279 MB STEP, immutable store objects, a fresh browser profile and the
frozen runtime fingerprint
`cbd6d3a33a5e4db8b2ca3f37a50ff4c3a24c1c376a1a58ea11f5c409d5878066`.
No task builds, tests or other task browsers ran concurrently.

| Hand, initial coarse display | Empty mesh index | Populated mesh index |
| --- | ---: | ---: |
| First geometry frame proxy | 2.12 s | 1.64 s |
| Complete rendered scene | 67.86 s | 18.77 s |
| Largest renderer peak RSS | 1,965 MiB | 1,890 MiB |
| Page exceptions / crashes | 0 / 0 | 0 / 0 |

The cold run's 866 resource-404 messages are tessellation-cache misses; the
cached run has none. Both drain worker ownership to zero. These are one
cold/cached pair with viewport refinement **disabled** and initial L0 detail;
they do not establish full-resolution loading or matched-quality speedup over
the historical 86–90-second baseline. The cold peak leaves only about 83 MiB
below 2 GiB, so memory headroom remains a concern despite completing this run.
The first-geometry metric is a publication-plus-draw frame proxy, not a GPU
completion fence; full-load polling has 500 ms granularity.

### Final integration warm check

[Final raw data](results/warm-integration-final-20260910.json) repeats the
original nine-part workload on the same frozen `cbd6d3a3…` runtime after the
task viewer and all test/browser activity stopped. Source restoration and
unchanged-runtime checks pass; startup remains excluded from warm timing.

| Workload, three samples | Median | Range |
| --- | ---: | ---: |
| Unchanged build | 18.46 ms | 17.27–71.76 ms |
| Geometry edit | 917.47 ms | 811.43–962.79 ms |
| Placement edit | 829.11 ms | 807.94–950.30 ms |
| Geometry preview publication | 528.78 ms | 441.73–583.17 ms |
| Placement preview publication | 440.32 ms | 440.17–458.54 ms |
| Repeated import, compile forbidden | 26.20 ms | 25.94–26.21 ms |

Cold import including process startup is 5.96 seconds. The visible run-to-run
variance and small sample count do not support a total warm-build speedup
claim. The 250 ms preview target still fails before browser display costs.
Recognizing multi-model imports fixes dependency reuse where existing model
boundaries allow it; this fixture still runs one whole model body.

### Saved-identity defect found after this study

A tiny [material identity probe](results/material-document-identity-20260910.json)
confirms an existing contract violation: changing only source PBR properties
changes the cached saved tree while producing identical STEP bytes. A cold
import loses that material and can also change occurrence names/grouping.
The later [implemented repair](SAVED-IDENTITY-FOLLOWUP.md) separates canonical
document trees from authored results and persists bound appearance without
requiring model-author cache/session utilities. The diagnostic remains the
original failure evidence. Performance measurements in the sections above
predate that repair; the new small-fixture study is recorded separately below.

The [80-box functional control](results/boxes-progress-diagnostic-20260910.json)
completed in about 1.93 seconds with monotonic 8 → 24 → 56 → 80 publication.
It is a cheap scheduler fixture, not a substitute for the hand milestone.

## Saved-document repair warm check

[Raw results](results/warm-appearance-20260910.json) repeat the same nine-part
fixture, three measured samples and primed variants on committed runtime
`858f718fc7c9be6df140443daf767a9aa67f75fb`. Runtime fingerprint:
`f47265c3decbf8ed665984eb0bfd4f8228f304c6e070efa75f9371139c9d07fb`.
All task builds, tests and browser runs had finished. The study used a separate
store under `models/tmp/perf-audit/appearance-store`. Exact source restoration
and unchanged-runtime checks pass. Startup, source writes, daemon IPC and
browser display remain outside the warm timing boundary.

| Workload, three samples | Median | Range |
| --- | ---: | ---: |
| Unchanged build | 18.81 ms | 17.40–19.25 ms |
| Geometry edit | 842.03 ms | 825.76–843.21 ms |
| Placement edit | 835.37 ms | 830.16–848.02 ms |
| Geometry preview publication | 452.36 ms | 438.33–457.51 ms |
| Placement preview publication | 451.26 ms | 447.58–459.72 ms |
| Geometry saved publication | 836.73 ms | 820.29–838.35 ms |
| Placement saved publication | 830.02 ms | 825.16–842.97 ms |
| Repeated import, compile forbidden | 25.87 ms | 25.86–26.89 ms |

The 2,268,663-byte baseline STEP retains SHA-256
`406dd2e19b6a4ea3a623abd19b03708b7d9e97b4402ba4161c737eb31f10fdca`.
The source and output geometry are unchanged by the cache/annotation repair.
Cold import, including its compile-process startup, is 5.95 seconds. The build
process high-water RSS reaches 596.39 MiB, including runtime/kernel state.
The first geometry edit spends 355 ms in the model body, 112 ms writing STEP,
243 ms reading it back and 9 ms packaging the canonical document tree. These
are rounded, nested stage observations, not independent totals.

The repair preserves cheap unchanged builds and parsed-document reuse on this
fixture. The small samples and variance in the earlier run do not establish a
complete warm-build speedup. Backend preview is still about 0.45 seconds, so
**the 250 ms target remains unmet before browser display costs**. The hand was
not rerun; earlier coarse hand results cannot be attributed to this runtime.

## Native OCCT experiment

This section retains the original mesher-v1 baseline. The
[mesher-v2 follow-up](MESHER-V2-20260910.md) fixes its measured JS quality flags
and adds final comparisons on curved, trimmed and singular surfaces.

[Merged results](results/native-20260910.json) and the six sibling JS, OCCT,
and decoder reports retain each component/LOD sample and its quality audit.
The runtime source digest was
`5810c50b038c5ef2ca40b034dd57db86e045eea95a8280d22793df8c8b166257`;
`runtimeUnchangedDuringStudy` is true. Both algorithms produced identical
bytes across all repetitions and the two fresh process trials.

Each component was sampled three times in each of two process trials. The
tables report the median of six sums over the nine components, rather than an
observed browser load or a single continuous assembly operation. Every sample
meshes from untriangulated inputs; OS filesystem caches are warm. Native uses
a fresh private BREP reconstruction, no parallel meshing, and the same physical
component-scale chord and 0.35-radian angular inputs as JS. The JS tessellator
version is 1 and the candidate transport codec is TESS v3.

### CPU stages

| Default chord 0.0015 | Current JS | Native experiment |
| --- | ---: | ---: |
| File read | 1.26 ms | 0.33 ms |
| SURF parse / BREP reconstruction | 6.05 ms | 4.70 ms |
| Tessellation kernel | 107.23 ms | 56.02 ms |
| Python arrays, normals, references | Included in JS tessellation | 51.73 ms |
| Encode | 0.85 ms | 2.79 ms |
| Full measured CPU pipeline | **115.17 ms** | **115.56 ms** |
| Pipeline range | 104.30–120.81 ms | 109.55–119.03 ms |
| Cached file read + JS decode | 1.24 ms | 1.15 ms |

Individual stage medians do not necessarily add to the median total. The full
pipeline excludes process startup, benchmark quality checks, temporary-file
writes, HTTP transport, worker messaging, main-thread work, and GPU upload.
At coarse chord 0.003, the equivalent pipeline medians are **134.65 ms JS** and
**113.54 ms native**. Current JS does not yield monotonic triangle counts or
timings for every component when changing chord alone; report the measured
settings and counts rather than assuming the coarse LOD is always cheaper.

| Payload / memory | JS 0.003 | Native 0.003 | JS 0.0015 | Native 0.0015 |
| --- | ---: | ---: | ---: | ---: |
| Unique-component triangles | 6,188 | 3,936 | 6,452 | 4,052 |
| Vertices | 7,441 | 5,068 | 7,714 | 5,184 |
| TESS bytes | 582,196 | 467,280 | 598,320 | 473,992 |
| Gzip bytes | 100,444 | 69,087 | 103,230 | 70,087 |

There are nine unique components and nine occurrences, so these counts also
equal this fixture's occurrence totals. This does not test hundreds of
instances sharing a single prototype. Node process peak RSS was 214.7–226.5 MiB;
the native process was 314.0–316.3 MiB. Those peaks include runtime imports,
both LODs, and audit work; they are not incremental mesh allocations. Gzip
sizes are potential transport costs, not measurements of browser/network time.

### Geometry and reference checks

All decoded face and triangle-side references are in range for both algorithms.
The native candidate contains all 582 face ordinals and all 1,692 edge
polylines; analytic normals are defined at every sampled node and have unit
length. Its triangle-side convention follows the production renderer's
opposite-vertex mapping. This checks payload structure and local references,
not interactive selection, screen-space edge appearance, or transformed picking.

| Default-LOD audit | Current JS | Native experiment |
| --- | ---: | ---: |
| Maximum component volume error | 0.986% | 1.011% |
| Maximum per-face area error | 0.449% | 1.011% |
| Degenerate triangles | 0 | 0 |
| Unmatched quantized mesh edges | 6 | 0 |
| Inconsistent welded-edge winding | 3 | 0 |
| Triangle/normal orientation flags | 32 | 0 |

Volume and area errors compare the triangulation with exact OCCT property
queries on the same BREP. The edge-weld check rounds positions on a grid of
`max(1e-7 mm, component.scale * 1e-6)`, so it is a diagnostic heuristic, not a
topological watertightness proof. Its JS edge/winding flags occur on the carrier;
normal flags also occur on gears. At coarse LOD, the JS audit reports four
degenerate triangles and 60 triangle/normal orientation flags. Those findings
warrant targeted investigation of the existing mesher, rather than an assumption
that its quality is already a reference standard.

Equal tolerance inputs produce different error/count tradeoffs. This study
does not establish matched visual quality, a common maximum surface-deviation
bound, or acceptable appearance at both LODs. It covers planar/cylindrical
trimming, holes and seams, but leaves B-splines, singular normals, mirrored
instances, and a wider STEP corpus for a future adoption gate.

### Ownership and decision

Native triangulation changed the private shape's canonical BREP serialization
hash for **all nine components**, even when serializing without triangles or
normals. Hashes stayed unchanged during the earlier exact-property queries and
the later array extraction; the change occurs during meshing. Every input BREP
file stayed byte-for-byte unchanged. Native meshing therefore must own a fresh
reconstruction and must not mutate a materialization shared with model execution
or component identity generation. The candidate never publishes to the store.

Keep the current JS mesher as the canonical implementation. A native replacement
would require a durable extraction implementation, verified face/edge/render
semantics, a broader matched-quality corpus, and explicit cache/export versioning.
A display-only implementation would additionally require a stated contract for
how display differs from export; neither scheduling nor memory pressure may
silently select different export bytes. Native candidates in this experiment
use private filenames, never the current mesher's cache key.

The default-LOD CPU delivery times are indistinguishable at this sample size,
native process memory is higher, and browser transport/main-thread/GPU costs
remain unmeasured here. Those costs should be measured independently before
crediting any kernel reduction as a viewer improvement. The evidence supports
retaining the current implementation and investigating the measured JS quality
flags separately.

## Reproduction checks

Both completed benchmark runs include unchanged-runtime checks. Warm execution
completed every build and restored its source. Native candidates passed the
production TESS decoder and repeated/fresh-process byte-determinism checks.
The setup command was exercised against the real fixture: it reproduced the
exact source bytes, refused overwrite, copied and verified 18 immutable objects
from the current store, and imported no OCCT modules. Python syntax and Node's
syntax check also pass. No runtime source, bundle, release version, or canonical
mesh index was changed by these benchmark commands.
