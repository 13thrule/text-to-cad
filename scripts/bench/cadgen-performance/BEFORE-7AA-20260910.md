# Reviewed-commit baseline — 2026-09-10

This measures the unmodified reviewed commit
`7aa3e85be76f305437abd3d7aba26e38b28e43cb`, reconstructed with read-only
`git archive`, against the same nine-part planetary source. It supersedes the
earlier two-sample baseline probe for this comparison. It does not describe
the subsequent working changes or a browser/FreeCAD GUI measurement.

[Raw JSON](results/warm-before-7aa-20260910.json) preserves all 20 build calls,
normal stage timings, events, operation-cache counters, identities, imported
module paths, archive proof, and the complete temporary benchmark adapter.
The sibling `warm-before-7aa-20260910-logs/` directory holds verbose output.

| Task | Median ms | Range ms | Samples |
| --- | ---: | ---: | ---: |
| Unchanged source | 17.62 | 17.01–18.75 | 3 |
| Carrier diameter 105 → 106 mm | 840.01 | 838.03–922.24 | 3 |
| Carrier placement −0.5 mm in Z | 844.72 | 835.65–849.97 | 3 |
| Read an already saved tree | 25.07 | 24.86–25.20 | 3 |
| Repeated foreign-STEP read, no document tree | 243.66 | 241.65–249.63 | 3 |

The build rows use the existing warm harness's priming and restoration
schedule, in one interpreter with build123d already imported. Source writes,
Python/kernel startup, daemon IPC and browser work are excluded. Every edit
re-executes the model and finishes STEP publication. The geometry edit replaces
one component ID; placement preserves all nine. Each measured edit reports
22 operation-cache hits, zero misses and zero errors.

The initial empty-store build took 5,193.47 ms after explicit kernel imports;
cadgen/build123d imports took 1,970.36 ms separately. These are single cold
observations, not a cold-start distribution or full process-launch timing.
Peak process RSS was 611,368,960 bytes (583.05 MiB), including imported kernel
and runtime state.

This revision emits neither early-preview nor saved-publication timing events.
Those fields remain null. The build-completion time must not be presented as
an observed visible-preview latency.

| Logged stage | Geometry median ms | Placement median ms |
| --- | ---: | ---: |
| Model body | 394 | 390 |
| STEP assembly/write | 118 | 119 |
| STEP readback | 236 | 235 |
| Component packaging, including STEP work | 377 | 378 |
| Write tree, including component packaging | 382 | 384 |

Stage logs are rounded and nested: do not add the packaging/tree rows to the
STEP rows. The 922.24 ms geometry sample is retained without filtering; its
logged generated stage was 828 ms and total stage 914 ms.

Saved-tree read samples forbid the STEP scene parser, raw import fallback and
compile submission. All pass. For the separate empty-store copy, the first
foreign read took 247.09 ms; that read and all three repeats invoked the STEP
parser once, submitted no compile, and left the document-tree index absent.
Thus these repeats measure parsing, not import cache hits. Every read returns
nine solids and volume `100976.60270468247 mm³`. The newer reader's first-time
compile/cache-publication cost must be compared with this different baseline
behavior explicitly.

The initial cold build reported five operation-cache errors without per-error
diagnostics in its normal log; the runner succeeded. All 20 builds exited zero,
all had nine components/occurrences, and all measured calls had zero cache
errors. No failed timed run was discarded.

## Provenance and reproduction

- Hardware: Apple M1 Max, 10 logical CPUs, 64 GiB RAM, macOS 26.5.1 arm64.
  The sandboxed hardware query returned unavailable; a read-only `sysctl`
  query after timing supplied CPU/RAM, recorded as a metadata supplement.
- Python 3.13.13; cadgen 0.5.1; build123d 0.11.1;
  cadquery-ocp 7.9.3.1.1; numpy 2.5.2; Node v26.7.0.
- Archive: `/private/tmp/cadgen-before-7aa.3YIWp2`. All 444 archived files were
  checked against the named commit's Git blob hashes. Package files were not
  edited. All cadgen module paths before and after the study point into this
  archive. `runtimeUnchangedDuringStudy` and `sourceRestored` are true.
- Runtime source digest:
  `cd405009b4828ccdf1edc4f555ed6ad75f6db8f59f6a25ce36fdaad245daab54`.
- Source SHA-256:
  `34b6ff6b337d8ee986e500b43a00102da421c868b61b39b678b4dc498554b64f`.
- Saved STEP: 2,268,663 bytes; SHA-256
  `406dd2e19b6a4ea3a623abd19b03708b7d9e97b4402ba4161c737eb31f10fdca`.
- Tree: `d34345bdc0a7a6a030f34af8872d6ad9a6ab43f24ca5bcfa83e00edb2e298e01`.
  These baseline STEP/tree identities match the earlier study's fixed input.
- No JS runtime was exercised by the STEP-only builds/imports. The Node
  resolver points into the baseline archive, and the raw metadata records
  archived packaged-runtime hashes; current working JS sources were not used.

Setup used `git archive` for `packages/cadgen`, `packages/cadgen-js`, root
guidance/version files and `scripts/bundle`, unpacked into that temporary
directory. The exact prepared fixture was copied into its `models/nine-part/`
directory. Current `warm_build.py` and `common.py` were copied into `bench/`
and adapted only for archive metadata and the older import behavior. Their
full executed source and hashes are embedded in the JSON. All generated CAD
and caches stayed under the archive's `models/`; the current fixture, runtime,
Git branch and bundles were untouched.

The exact timing command was:

```sh
PYTHONPATH=/private/tmp/cadgen-before-7aa.3YIWp2/packages/cadgen/src \
PYTHONDONTWRITEBYTECODE=1 \
/Users/jakefitzgerald/robots/text-to-cad/.venv/bin/python \
  /private/tmp/cadgen-before-7aa.3YIWp2/bench/warm_build.py \
  --model /private/tmp/cadgen-before-7aa.3YIWp2/models/nine-part/planetary_gear_assembly.py \
  --store /private/tmp/cadgen-before-7aa.3YIWp2/models/nine-part/store \
  --report /Users/jakefitzgerald/.codex/worktrees/3c6b/text-to-cad/scripts/bench/cadgen-performance/results/warm-before-7aa-20260910.json \
  --iterations 3
```

Other workers paused compute for these samples. Subsequent source edits in the
main worktree cannot change the archived baseline. A final AFTER study and the
separate retained-document FreeCAD diagnostic should name their own timing
boundaries alongside these results; this record makes no parity claim.
